taskkill /f /im python.exe
C:\agentcoder
venv\Scripts\activate
uvicorn api.main:app --reload




venv\Scripts\activate
npm run dev



Your registry URI is 176980002446.dkr.ecr.ap-south-1.amazonaws.com/agentcoder



Here's the full startup guide:

---

## Starting AgentCoder from scratch

### Option A — Run locally (development)

**Step 1 — Activate venv**
```bash
cd C:\agentcoder
venv\Scripts\activate
```

**Step 2 — Start the backend**
```bash
uvicorn api.main:app --reload
```

**Step 3 — Open a second terminal, activate venv, start frontend**
```bash
cd C:\agentcoder
venv\Scripts\activate
streamlit run frontend\app.py
```

**Step 4 — Open browser**
- UI: `http://localhost:8501`
- API docs: `http://localhost:8000/docs`

---

### Option B — Run with Docker (production-like)

**Step 1 — Start Docker Desktop** (wait for it to fully load)

**Step 2 — Run containers**
```bash
cd C:\agentcoder
docker-compose up
```

**Step 3 — Open browser**
- UI: `http://localhost:8501`
- API docs: `http://localhost:8000/docs`

To stop: `Ctrl+C` then `docker-compose down`

---

### Option C — AWS (already live)

Your app is already running at `http://52.66.191.149:8501`

If it goes down (e.g. task stopped), restart it:
```bash
aws ecs update-service --cluster agentcoder-cluster --service agentcoder-service --force-new-deployment --region ap-south-1
```

---

Save this somewhere. Want to do the README + GitHub push next?